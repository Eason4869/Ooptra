# 可选依赖，只在使用对应功能时才需要安装。

# WebUI「Oopz 登录」-> 方式二：网页版登录（Playwright 打开 Oopz 网页版抓取凭据）
#   pip install -r requirements-optional.txt
#   python -m playwright install chromium
playwright>=1.40,<2

# OOPZ_CONFIG.proxy 填 socks5://... 时才需要（默认只有 http/https 代理）
aiohttp-socks>=0.10,<1
python-socks>=2.5,<3
