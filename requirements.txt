# 运行时依赖（纯桥接：Oopz 事件 -> OneBot v11 反向 WebSocket）

# HTTP / WebSocket（SDK 的 REST 与事件 WS，OneBot 反向 WS，Web 控制台）
aiohttp>=3.9,<4
# OneBot v11 的 ID/消息映射库、迁移
aiosqlite>=0.20,<1
# 账号密码登录的 RSA 加解密、private_key.py
cryptography>=42,<48
# 配置与事件模型
pydantic>=2.0,<3
# 图片消息的格式识别（发送 CQ:image 时用）
pillow>=10,<13
# 账号密码登录接口（同步 HTTP 客户端）
requests>=2.32,<3
# 默认请求头声明了 br/zstd，需要它们才能解压响应
brotli>=1.2,<2
zstandard>=0.23,<1
